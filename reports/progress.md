# 项目进度

更新：2026-09-17。当前 M1 进行中，M2–M6 未开始。

最新完成 **M1-004 最小业务规格与数据检查**，累计 **9/31** 项。可运行数据检查命令查看四类任务的手工预期；业务规则执行、数据库、API/MCP 和训练尚未实现。下一步为 M1-007 契约，再推进 M1-008 持久化。

## 2026-09-17：M1-002 配置模块及此前交付记录

本轮仅完成 **M1-002 应用配置与显式路径**。首批的轻量环境与源码打包成果保留；没有推进业务规则、API、MCP 或模型功能。

已完成 M1-001、M1-002、M1-003、M1-021、M1-022、M1-023、M1-027、M1-030，共 8/31 项。2026-09-17 权限回归曾重新打开 M1-022/027/030，修复并验证后恢复勾选。M1-028 的业务启动/演示部分尚缺，继续保持未勾选；M1 整体验收尚未完成。

本轮本地轻量已通过（Codex，Windows，Python 3.12.13）：`uv run --no-sync --cache-dir .uv-cache python -m pytest -q tests/unit/test_settings.py` → **9 passed in 0.93s**。覆盖 YAML/.env/进程变量优先级、读取不改环境变量、跨工作目录与外部绝对路径、目录初始化、不创建数据库、无效配置和缺失文件。实际运行 `python -m tool_agent_lab.settings --init-dirs`，生成运行/日志目录并输出解析路径，证据为 `artifacts/M1/settings-check.json`。

新增 `.env.example`、`configs/app.yaml`、`src/tool_agent_lab/settings.py`、`tests/unit/test_settings.py`；更新 `pyproject.toml`/`uv.lock` 和打包规则的必需资源。`.gitignore` 沿用已检查的环境、`.env`、运行产物排除规则。新增依赖只有 Pydantic、PyYAML、python-dotenv 及其轻量传递依赖；安装前检查锁文件，不含 torch 或训练推理栈。

此前打包测试结果为 **12 passed**，本轮没有重跑该套件，只对更新后的源码包做一次交付检查。历史环境/权限检查与本轮细节见 [M1 局部验证](M1/acceptance.md)。

权限回归：原先在私有临时目录生成 ZIP 后直接移动，导致普通桌面账号无权读取。现有两个 ZIP 及校验文件已逐一恢复目录默认权限，内容哈希未变；用户通过 WinRAR 确认“现在能正常打开”。脚本改用普通唯一暂存目录自然继承输出目录 ACL，新测试先在旧实现上复现失败，再在修复后通过。修复记录为 `artifacts/M1/permissions-repair.json`，新包实际权限检查保存在交付摘要中。

解释器位于项目 `artifacts/tooling/python/`，`.venv` 与 `.uv-cache` 也在项目内；未更改系统/用户 PATH 或注册表。实际环境检查保存于 `artifacts/M1/environment.json`，未发现 torch。

源码包已完成真实预览、生成和本地新目录解压检查，包内逐文件/整包哈希、解压后的标准库 CLI 和源码导入均通过。当前包名、SHA-256、大小、文件数、内容版本及本地解压目录统一记录在 `artifacts/M1/latest-delivery.json`；预览为 `artifacts/M1/package-preview.json`，包内为 `PACKAGE_MANIFEST.json`。文档变更后重新生成并核验交付包；包摘要留在归档外，避免包内文档引用自身哈希。

服务器未连接、尚未更新；真实远程解压启动待 M6 验证。无模型、run 或训练产物，无效果数字。

下一步：

1. M1-004：先定义少量订单与四类任务规格。
2. M1-007：补任务身份、提案、确认和执行上下文契约。
3. M1-008：使用配置中的显式路径建立数据库与初始迁移，继续推进最短退款演示。

## 2026-09-17：补充开发与进度记录流程（仅文档）

- 在工作区 `AGENTS.md` 明确每轮只推进一个小功能/模块/方向，按影响范围执行必要测试，并汇报任务指标、测试结果和文件变更。
- 本轮进一步明确：编码前回顾本报告中的历史问题与下一步，任务后按日期和任务编号增量更新；允许整理历史内容，但须保留问题原因、修复方法、必要命令、环境和验证证据，不能用最新摘要覆盖历史。
- 验证：回读相关文档，确认新约定已写入，既有环境、配置、权限回归及交付记录保留。仅修改文档，未运行代码测试或重新打包；现有源码包对应此前交付，本条新增记录尚未纳入包内。
- M1 仍为 8/31 项，本轮没有新增代码任务完成项。下一步仍为 M1-004 少量订单与四类任务规格。

## 2026-09-17：M1-004 最小业务规格与数据检查

- 完成 `data/business/v1/` 的规格说明、规则参数、4 个订单、1 笔已确认历史退款和 5 个独立示例，覆盖四类任务。固定业务时间为 `2026-09-17T12:00:00+08:00`，金额使用 CNY 整数分；`scripts/check_business_data.py` 从现有配置读取，检查数据一致性并展示手工预期。
- 本地轻量验证（Codex，Windows，项目 Python 3.12.13）：`uv run --no-sync --cache-dir .uv-cache python -m pytest -q tests/unit/test_business_data.py` → **9 passed in 0.48s**。覆盖四类样例、真实时间差事实、浮点/布尔/负数金额、缺失退款账本、确认金额不符、重复退款期望、身份不符及跨工作目录只读入口。没有新增依赖；旧配置 9 项、旧打包 12 项测试本轮未重跑。
- 实际运行 `uv run --no-sync --cache-dir .uv-cache python scripts/check_business_data.py`，输出 `fixture_check_passed`、4 个订单、5 个示例、四类任务和 `business_execution: not_run`。实际命令、输出、数据文件哈希及执行环境保存在 `artifacts/M1/business-data-check.json`。
- 数据设计中的关键区别：ORD-1003 初始退款为 9900 分，必须有同额历史操作及绑定确认；该场景预期新增写入为 0。两个 ORD-1001 样例独立重置，避免执行顺序污染结果。`expected` 和尚未发生的澄清回复不进入模型初始输入。这些是规格与数据约束，尚不能作为业务幂等、授权或故障恢复已实现的证据。
- 本轮无新增实跑故障。打包规则将上述资源和检查入口列为必需项；交付继续使用已修复的普通目录权限继承，按原记录保留 Windows ACL 问题原因和修复证据。新包按预览、生成、校验、解压检查流程交付，结果见 `artifacts/M1/latest-delivery.json`；服务器仍未连接。
- 同步任务清单、AGENTS、方案、实施计划、README 和 M1 局部验证。只新增 M1-004 完成项，累计 9/31；M1-005/006、M1-010/024 及整阶段验收仍未完成。下一步先做 M1-007，随后 M1-008，继续最短退款流程。
